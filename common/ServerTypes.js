/**
 * 服务器类型（接口BaseURL不同）
 * @enum {'DEFAULT' | 'USER'}
 */
const ServerTypes = {
  /** @type {'DEFAULT'} */
  DEFAULT: 'DEFAULT',
  /** @type {'USER'} */
  USER: 'USER',
}

export default ServerTypes
